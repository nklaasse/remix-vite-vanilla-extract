import { assignVars } from "@vanilla-extract/css";
import * as colors from "../colors.css";

export const accents = {
  // neutral: {
  //   vars: assignVars(colors.base.accent, colors.accents.neutral),
  // },
  brand: {
    vars: assignVars(colors.base.brand, colors.accents.brand),
  },
  // selection: {
  //   vars: assignVars(colors.base.accent, colors.accents.selection),
  // },
  // tomato: {
  //   vars: assignVars(colors.base.accent, colors.accents.tomato),
  // },
  // indigo: {
  //   vars: assignVars(colors.base.accent, colors.accents.indigo),
  // },
  // yellow: {
  //   vars: assignVars(colors.base.accent, colors.accents.yellow),
  // },
  // amber: {
  //   vars: assignVars(colors.base.accent, colors.accents.amber),
  // },
};
