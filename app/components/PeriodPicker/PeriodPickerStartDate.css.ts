import { style } from "@vanilla-extract/css";

const periodPickerStartDateContainer = style({
  gridArea: "startDate",
});

export const periodPickerStartDate = {
  container: periodPickerStartDateContainer,
};
