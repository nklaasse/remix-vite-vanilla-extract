import { style } from "@vanilla-extract/css";

const periodPickerCurrentContainer = style({
  gridArea: "current",
});

export const periodPickerCurrent = { container: periodPickerCurrentContainer };
